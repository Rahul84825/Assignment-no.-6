# Assignment 6 – Simple Web Server using Node.js

## 📌 Project Overview
This project is part of **Assignment 6**, where a simple web server is created using **Node.js** and its built-in **http module**.  
The server handles multiple routes and serves corresponding HTML pages with proper HTTP status codes.

---

## ⚙️ Technologies Used
- Node.js
- Built-in `http` module
- Built-in `fs` module
- HTML
- CSS
- npm
- Git & GitHub

---

## 📂 Project Structure

---

## 🚀 Features Implemented
- HTTP server created using Node.js `http` module
- Server listens on port **3000**
- Routing implemented for:
  - `/home`
  - `/about`
  - `/contact`
- External CSS file for styling all pages
- Asynchronous file handling using `fs.readFile`
- Proper HTTP status codes:
  - `200 OK`
  - `404 Not Found`
  - `500 Internal Server Error`
- Custom 404 handling for invalid routes

---

## ▶️ How to Run the Project

### Method 1: Using npm (Recommended)
```bash
npm run dev
```

